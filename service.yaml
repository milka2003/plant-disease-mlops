apiVersion: v1
kind: Service

metadata:
  name: plant-disease-service

spec:
  selector:
    app: plant-disease-api

  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000

  type: NodePort